package array;

import java.util.ArrayList;
import java.util.Collections;
import java.util.List;

public class MissingNumberInaRange {

	/*
	 * static int missingNumber(int arr[] ,int n) { List<Integer> list=new
	 * ArrayList<>(); for(int i:arr) { list.add(i); } int
	 * minValue=Collections.min(list);
	 * 
	 * }
	 */
}
